require "spec_helper"

RSpec.describe Capistrano::NetStorage do
  it "has a version number" do
    expect(Capistrano::NetStorage::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
